import{u as e}from"./index-CnrUXjyz.js";var t=[`markdown`,`ppt`,`reveal`,`pretty`,`eli5`,`eli_ms`,`eli_eng`,`parable`,`feynman`];function n(e,n,r=t,i=10,a=[]){let o=e.trim();if(!o&&!n?.trim())throw Error(`资料文本或标题提示至少填一项`);let s=Math.max(1,Math.min(25,Math.floor(i)||1)),c=r.join(`、`),l=a.length>0?`\n【已有卡片标题】请避免生成标题或语义高度相似的卡片：\n${a.slice(-60).map((e,t)=>`${t+1}. ${e}`).join(`
`)}\n`:``;return[`【使用说明】`,`请把下面的指令和资料一起发送给 AI，然后把 AI 返回的 JSON 粘贴回卡片学习系统。`,`你可以使用任何 AI 聊天工具（ChatGPT、Claude、豆包、文心一言、Kimi 等）。`,``,`--- 以下是发送给 AI 的提示词 ---`,``,`${[`你是教学写作助手。根据用户提供的「资料」和「可选标题」，生成 N 张 markdown 学习卡片。`,`【输出格式】严格 JSON 对象，不要任何解释、不要 markdown 代码块、不要思考块。`,`JSON 顶层结构：`,`  { "cards": [ { "title": "...", "kind": "...", "markdown": "..." }, ... ] }`,`每张卡片字段：`,`  - title: string，<= 30 字，简明扼要，避免与已有卡片标题重复`,`  - kind: 枚举值之一（见下方 kind 白名单）；可省略，省略时按 markdown 处理`,`  - markdown: 纯 markdown 文本（# ## ### 分级 + 列表 + 引用 + 表格等），150–600 字`,`  - theme: 可选；仅在 kind ∈ {pretty, reveal, parable} 时有意义。自由给一个独特的主题名（中英文皆可，2-6 个词），后续查看时会按主题渲染视觉风格；不要从固定的几个值里挑，要尽量发散。`,`  - concept: 可选；仅在 kind=parable 时有意义`,`【kind 白名单】用户已限定：${c}。不要输出白名单外的 kind。`,`【卡片数量】请生成**正好 ${s} 张**卡片。资料不足时围绕同一主题的不同侧面（定义/例子/对比/应用/反例/常见误区）展开，绝不偷工减料。`+l,`【绝对禁止】JSON 之外的内容、注释、思考过程。`,`【必须】第一个字符是 {，最后一个字符是 }。`].join(`
`)}\n\n${o?`# 资料\n\n"""${o.slice(0,16e3)}"""`:`# 资料
（用户未提供）
请根据下面的「标题提示」自由发挥，围绕该主题挑选适合学习的知识点作为卡片。`}${n?.trim()?`\n【用户提示的标题方向】${n.trim()}（仅作参考）`:``}\n\n请严格按 system prompt 输出 JSON。`].join(`
`)}function r(n,r=t){return e(n,r)}export{r as n,n as t};